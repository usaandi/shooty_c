#ifndef SOLDIERTYPE_HPP
#define SOLDIERTYPE_HPP

namespace config {
    enum class SoldierType {
        INFANTRY,
        HEAVY,
        SNIPER,
        MEDIC
        // Add more soldier types as needed
    };

} // namespace config

#endif //SOLDIERTYPE_HPP